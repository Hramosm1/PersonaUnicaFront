import { CamposDirective } from './campos.directive';

describe('CamposDirective', () => {
	it('should create an instance', () => {
		const directive = new CamposDirective();
		expect(directive).toBeTruthy();
	});
});
